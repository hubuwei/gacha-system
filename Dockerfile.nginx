FROM nginx:latest

LABEL maintainer="gacha-system"
LABEL description="Nginx for gacha-system"

COPY nginx-config.conf /etc/nginx/conf.d/default.conf

EXPOSE 80 443

HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD curl -f http://localhost/ || exit 1