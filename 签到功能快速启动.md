# 🚀 每日签到功能 - 快速启动指南

## 第一步：安装并启动 Redis

### Windows 系统
1. 下载 Redis Windows 版本
   - 下载地址：https://github.com/microsoftarchive/redis/releases
   - 选择 `Redis-x64-3.0.504.msi`

2. 安装后启动 Redis
   ```bash
   # 默认端口：6379
   redis-server.exe redis.conf
   ```

3. 验证 Redis 是否运行
   ```bash
   redis-cli ping
   # 应返回：PONG
   ```

## 第二步：创建数据库表

```bash
mysql -u root -p gacha_system < database/create_checkin_table.sql
```

或直接执行 SQL：
```sql
CREATE TABLE IF NOT EXISTS daily_check_in_records (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '用户 ID',
    check_in_date DATE NOT NULL COMMENT '签到日期',
    reward_points INT DEFAULT 0 COMMENT '奖励积分',
    reward_balance DECIMAL(10,2) DEFAULT 0.00 COMMENT '奖励虚拟货币',
    consecutive_days INT DEFAULT 1 COMMENT '连续签到天数',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_date (user_id, check_in_date),
    INDEX idx_user_id (user_id),
    INDEX idx_check_in_date (check_in_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

## 第三步：编译项目

```bash
cd E:\CFDemo\gacha-system\game-service
mvn clean package
```

## 第四步：测试 Redis 连接（可选）

```bash
# 运行 Redis 测试应用
java -cp target/game-service-1.0.0-SNAPSHOT.jar com.cheng.game.RedisTestApplication
```

如果看到以下输出，说明 Redis 连接成功：
```
========== Redis 连接测试 ==========
✅ 写入成功：test:key = Hello Redis
✅ 读取成功：test:key = Hello Redis
✅ 删除成功：test:key

🎉 Redis 连接正常！
==============================
```

## 第五步：启动 game-service

```bash
cd E:\CFDemo\gacha-system\game-service
java -jar target/game-service-1.0.0-SNAPSHOT.jar
```

启动成功后应看到：
```
Started GameServiceApplication in X.XXX seconds
```

## 第六步：测试签到功能

### 方法一：使用 HTML 测试页面
1. 打开浏览器
2. 访问：`file:///E:/CFDemo/gacha-system/checkin.html`
3. 输入用户 ID（如：1）
4. 点击"查询签到状态"
5. 点击"立即签到"

### 方法二：使用 cURL 命令
```bash
# 查询签到状态
curl http://localhost:8082/api/check-in/status?userId=1

# 执行签到
curl -X POST http://localhost:8082/api/check-in ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\": 1}"
```

### 方法三：使用 Postman
1. **查询接口**
   - Method: GET
   - URL: http://localhost:8082/api/check-in/status?userId=1

2. **签到接口**
   - Method: POST
   - URL: http://localhost:8082/api/check-in
   - Headers: Content-Type: application/json
   - Body: {"userId": 1}

## 第七步：验证结果

### 查看 Redis 数据
```bash
# 进入 Redis CLI
redis-cli

# 查看今日签到记录
KEYS check_in:1:*

# 查看连续签到天数
GET consecutive_days:1
```

### 查看数据库记录
```bash
mysql -u root -p

USE gacha_system;
SELECT * FROM daily_check_in_records ORDER BY created_at DESC LIMIT 10;
```

## 🎯 测试连续签到逻辑

### 模拟连续签到
```bash
# 第 1 天签到
curl -X POST http://localhost:8082/api/check-in -H "Content-Type: application/json" -d "{\"userId\": 1}"

# 修改数据库中的日期（模拟昨天签到）
mysql -u root -p gacha_system -e "UPDATE daily_check_in_records SET check_in_date = DATE_SUB(CURDATE(), INTERVAL 1 DAY) WHERE user_id = 1;"

# 再次签到，连续天数应该 +1
curl -X POST http://localhost:8082/api/check-in -H "Content-Type: application/json" -d "{\"userId\": 1}"
```

### 测试特殊奖励
连续签到到第 7 天和第 30 天时，会触发额外奖励提示。

## ⚠️ 常见问题排查

### 问题 1：Redis 连接失败
**错误信息**：`Cannot get Jedis connection`

**解决方案**：
```bash
# 检查 Redis 是否运行
redis-cli ping

# 如果没有响应，启动 Redis
redis-server.exe redis.conf
```

### 问题 2：端口冲突
**错误信息**：`Port 6379 is already in use`

**解决方案**：
```bash
# Windows：查看占用端口的进程
netstat -ano | findstr :6379

# 杀死进程
taskkill /F /PID <进程 ID>
```

### 问题 3：数据库表不存在
**错误信息**：`Table 'gacha_system.daily_check_in_records' doesn't exist`

**解决方案**：
```bash
# 重新执行建表语句
mysql -u root -p gacha_system < database/create_checkin_table.sql
```

### 问题 4：CORS 跨域错误
**解决方案**：已在配置文件中启用 CORS，确保前端请求的 URL 正确。

## 📊 预期效果

### 首次签到
```json
{
    "success": true,
    "message": "签到成功！",
    "consecutiveDays": 1,
    "rewardPoints": 10,
    "rewardBalance": 1.0,
    "nextBonusDays": 6
}
```

### 第 7 天签到
```json
{
    "success": true,
    "message": "签到成功！已连续签到 7 天",
    "consecutiveDays": 7,
    "rewardPoints": 98,
    "rewardBalance": 11.0,
    "bonusMessage": "恭喜！连续签到 7 天，额外获得 88 积分和 10.0 元！",
    "nextBonusDays": 23
}
```

### 第 30 天签到
```json
{
    "success": true,
    "message": "签到成功！已连续签到 30 天",
    "consecutiveDays": 30,
    "rewardPoints": 898,
    "rewardBalance": 101.0,
    "bonusMessage": "恭喜！连续签到 30 天，获得 888 积分、100.0 元和 手机兑换券！",
    "nextBonusDays": 30
}
```

## 🔧 配置文件位置

- Redis 配置：`game-service/src/main/resources/application.yml`
- Redis 连接类：`game-service/src/main/java/com/cheng/gacha/config/RedisConfig.java`
- 服务实现：`game-service/src/main/java/com/cheng/game/service/impl/CheckInServiceImpl.java`

## 📝 下一步建议

1. **集成 JWT 认证**：在现有系统中添加用户认证
2. **添加签到排行榜**：按连续签到天数排名
3. **实现补签功能**：允许用户每月有 1-2 次补签机会
4. **数据统计**：记录用户总签到次数、最长连续签到等
5. **推送提醒**：每天 0 点推送签到提醒

## 🎉 完成！

现在你已经成功部署了每日签到功能！

测试账号可以参考 init.sql 中的 test001（userId: 1）和 test002（userId: 2）。
