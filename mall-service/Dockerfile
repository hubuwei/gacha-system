# ============================================
# Mall Service Dockerfile (基于现有镜像)
# ============================================

FROM gacha-system-mall-service:latest
WORKDIR /app
COPY target/*.jar app.jar
